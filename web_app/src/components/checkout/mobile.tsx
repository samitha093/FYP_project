import React from 'react';

interface AppProps {
    darkMode: boolean;
}

const Mobile: React.FC<AppProps> = ({ darkMode }) => {
    return (
        <>
            mobile
        </>
    );
};

export default Mobile;
